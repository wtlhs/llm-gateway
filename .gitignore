# 构建产物
*.exe
llm-gateway
*.test

# Go
vendor/
*.out

# 环境配置(含密码, 真实文件不入库; 只提交 .env*.example 模板)
.env
.env.deploy
.env.*.local
dist/

# 前端构建产物与依赖
web/node_modules/
web/dist/

# IDE
.vscode/
.idea/
*.swp

# OS
.DS_Store
Thumbs.db
llm-gateway.tar
web/tsconfig.tsbuildinfo

# 交叉编译产物(本地构建用, 不入库)
llm-gateway-linux


# llm-platform 交叉编译产物与 embed 构建产物(只保留 .gitkeep)
platform-linux
internal/platform/web_dist/*
!internal/platform/web_dist/.gitkeep
